# Entorno LOCAL: lo usa `pnpm dev`.
# Se versiona a propósito, así el equipo lo tiene funcionando sin configurar nada.
# Si necesitás apuntar a otro lado solo para vos, creá un `.env.local`
# (tiene prioridad sobre este y no se versiona).
NEXT_PUBLIC_API_URL=http://localhost:8000
