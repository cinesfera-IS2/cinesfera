# Plantilla de variables del frontend.
# Copiala a `.env.local` (que NO se versiona) y ajustá los valores:
#   cp .env.example .env.local

# URL del backend. En local apuntá a tu FastAPI:
NEXT_PUBLIC_API_URL=http://localhost:8000
